name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Build and publish release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Zip the integration
        working-directory: custom_components/airplay_notifier
        run: zip -r ${{ github.workspace }}/airplay_notifier.zip . -x '.*'

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: airplay_notifier.zip
          generate_release_notes: true
